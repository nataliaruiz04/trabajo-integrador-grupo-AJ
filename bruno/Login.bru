meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ferben@correo.com",
    "contrasenia": "ferben"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
